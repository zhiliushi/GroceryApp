"""Operational scripts (migrations, backfills, integrity checks).

Modules here can be invoked via `python -m scripts.<name>` from the
`backend/` directory, OR imported by `main.py` startup hooks for
idempotent runtime backfills.
"""
